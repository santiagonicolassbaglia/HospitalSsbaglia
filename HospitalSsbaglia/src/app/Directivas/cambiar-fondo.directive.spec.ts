import { CambiarFondoDirective } from './cambiar-fondo.directive';

describe('CambiarFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarFondoDirective();
    expect(directive).toBeTruthy();
  });
});
