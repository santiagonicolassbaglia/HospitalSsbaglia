import { FormatearNumerosDirective } from './formatear-numeros.directive';

describe('FormatearNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatearNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
