import { ModoOscuroDirective } from './modo-oscuro.directive';

describe('ModoOscuroDirective', () => {
  it('should create an instance', () => {
    const directive = new ModoOscuroDirective();
    expect(directive).toBeTruthy();
  });
});
