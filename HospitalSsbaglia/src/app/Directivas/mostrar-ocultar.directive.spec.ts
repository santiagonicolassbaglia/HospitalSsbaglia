import { MostrarOcultarDirective } from './mostrar-ocultar.directive';

describe('MostrarOcultarDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarOcultarDirective();
    expect(directive).toBeTruthy();
  });
});
