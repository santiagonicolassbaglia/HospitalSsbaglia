import { ResaltaTextoDirective } from './resalta-texto.directive';

describe('ResaltaTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltaTextoDirective();
    expect(directive).toBeTruthy();
  });
});
