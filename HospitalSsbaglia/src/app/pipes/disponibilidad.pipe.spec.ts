import { DisponibilidadPipe } from './disponibilidad.pipe';

describe('DisponibilidadPipe', () => {
  it('create an instance', () => {
    const pipe = new DisponibilidadPipe();
    expect(pipe).toBeTruthy();
  });
});
