import { FiltrarAdminPipe } from './filtrar-admin.pipe';

describe('FiltrarAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
