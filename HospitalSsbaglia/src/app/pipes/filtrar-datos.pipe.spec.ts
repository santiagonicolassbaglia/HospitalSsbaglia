import { FiltrarDatosPipe } from './filtrar-datos.pipe';

describe('FiltrarDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
