import { FiltrarPorEstadoPipe } from './filtrar-por-estado.pipe';

describe('FiltrarPorEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
