import { OrdenarDatosPipe } from './ordenar-datos.pipe';

describe('OrdenarDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
