import { OrdenarDiasPipe } from './ordenar-dias.pipe';

describe('OrdenarDiasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarDiasPipe();
    expect(pipe).toBeTruthy();
  });
});
