import { OrdenarHistoriasPipe } from './ordenar-historias.pipe';

describe('OrdenarHistoriasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarHistoriasPipe();
    expect(pipe).toBeTruthy();
  });
});
