import { OrdenarTurnosPipe } from './ordenar-turnos.pipe';

describe('OrdenarTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
